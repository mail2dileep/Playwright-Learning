import { Page, Locator } from '@playwright/test';\n\nexport class EnergyCostCalculatorPage {\n  private readonly page: Page;\n\n  // Locators\n  private readonly monthSelect: Locator;\n  private readonly previousReadInput: Locator;\n  private readonly currentReadInput: Locator;\n  private readonly estimatedElectricUseInput: Locator;\n  private readonly estimatedGasUseInput: Locator;\n  private readonly electricServiceTypeRadio: Locator;\n  private readonly electricAndGasServiceTypeRadio: Locator;\n  private readonly howToReadYourBillButton: Locator;\n  private readonly howToFindUsageButton: Locator;\n  private readonly resetButton: Locator;\n  private readonly calculateButton: Locator;\n\n  constructor(page: Page) {\n    this.page = page;\n    this.monthSelect = page.getByLabel('Month');\n    this.previousReadInput = page.getByLabel('Enter Previous Read:');\n    this.currentReadInput = page.getByLabel('Enter Current Read:');\n    this.estimatedElectricUseInput = page.getByLabel('Estimated Electric use (kWh):');\n    this.estimatedGasUseInput = page.getByLabel('Estimated Gas use (Ccf):');\n    this.electricServiceTypeRadio = page.locator('#e');\n    this.electricAndGasServiceTypeRadio = page.locator('#eg');\n    this.howToReadYourBillButton = page.locator('#howToReadYourBillBtn');\n    this.howToFindUsageButton = page.locator('#howToFindUsageBtn');\n    this.resetButton = page.locator('#rateCalCancelBtn');\n    this.calculateButton = page.locator('#validateMoveInBtn');\n  }\n\n  // Actions\n  /**\n   * Navigates to the specified URL.\n   * @param url The URL to navigate to.\n   */\n  async navigateTo(url: string): Promise<void> {\n    await this.page.goto(url);\n  }\n\n  /**\n   * Selects an option from the month dropdown.\n   * @param monthValue The value of the month to select (e.g., 'm06' for June).\n   */\n  async selectMonth(monthValue: string): Promise<void> {\n    await this.monthSelect.selectOption(monthValue);\n  }\n\n  /**\n   * Enters a value into the previous meter read input field.\n   * @param readValue The previous meter read value.\n   */\n  async enterPreviousMeterRead(readValue: string): Promise<void> {\n    await this.previousReadInput.fill(readValue);\n  }\n\n  /**\n   * Enters a value into the current meter read input field.\n   * @param readValue The current meter read value.\n   */\n  async enterCurrentMeterRead(readValue: string): Promise<void> {\n    await this.currentReadInput.fill(readValue);\n  }\n\n  /**\n   * Selects the 'Electric' service type radio button.\n   */\n  async selectElectricServiceType(): Promise<void> {\n    await this.electricServiceTypeRadio.click();\n  }\n\n  /**\n   * Selects the 'Electric and Gas' service type radio button.\n   */\n  async selectElectricAndGasServiceType(): Promise<void> {\n    await this.electricAndGasServiceTypeRadio.click();\n  }\n\n  /**\n   * Clicks the 'Calculate' button.\n   */\n  async clickCalculateButton(): Promise<void> {\n    await this.calculateButton.click();\n  }\n\n  /**\n   * Clicks the 'Reset' button.\n   */\n  async clickResetButton(): Promise<void> {\n    await this.resetButton.click();\n  }\n\n  // Verification methods\n  /**\n   * Checks if the month dropdown is visible.\n   * @returns A promise that resolves to true if the dropdown is visible, false otherwise.\n   */\n  async isMonthDropdownVisible(): Promise<boolean> {\n    return await this.monthSelect.isVisible();\n  }\n\n  /**\n   * Checks if the 'Enter Previous Read' input field is visible.\n   * @returns A promise that resolves to true if the field is visible, false otherwise.\n   */\n  async isPreviousReadFieldVisible(): Promise<boolean> {\n    return await this.previousReadInput.isVisible();\n  }\n\n  /**\n   * Checks if the 'Enter Current Read' input field is visible.\n   * @returns A promise that resolves to true if the field is visible, false otherwise.\n   */\n  async isCurrentReadFieldVisible(): Promise<boolean> {\n    return await this.currentReadInput.isVisible();\n  }\n\n  /**\n   * Checks if the 'Estimated Electric use (kWh)' input field is visible.\n   * @returns A promise that resolves to true if the field is visible, false otherwise.\n   */\n  async isEstimatedElectricUseFieldVisible(): Promise<boolean> {
    return await this.estimatedElectricUseInput.isVisible();
  }

  /**\n   * Checks if the 'Estimated Gas use (Ccf)' input field is visible.\n   * @returns A promise that resolves to true if the field is visible, false otherwise.\n   */\n  async isEstimatedGasUseFieldVisible(): Promise<boolean> {\n    return await this.estimatedGasUseInput.isVisible();\n  }\n\n  /**\n   * Checks if the 'Estimated Gas use (Ccf)' input field is disabled.\n   * @returns A promise that resolves to true if the field is disabled, false otherwise.\n   */\n  async isEstimatedGasUseFieldDisabled(): Promise<boolean> {\n    return await this.estimatedGasUseInput.isDisabled();\n  }\n\n  /**\n   * Checks if the 'Electric' service type radio button is visible.\n   * @returns A promise that resolves to true if the radio button is visible, false otherwise.\n   */\n  async isElectricServiceTypeRadioVisible(): Promise<boolean> {\n    return await this.electricServiceTypeRadio.isVisible();\n  }\n\n  /**\n   * Checks if the 'Electric and Gas' service type radio button is visible.\n   * @returns A promise that resolves to true if the radio button is visible, false otherwise.\n   */\n  async isElectricAndGasServiceTypeRadioVisible(): Promise<boolean> {\n    return await this.electricAndGasServiceTypeRadio.isVisible();\n  }\n\n  /**\n   * Checks if the 'Calculate' button is visible.\n   * @returns A promise that resolves to true if the button is visible, false otherwise.\n   */\n  async isCalculateButtonVisible(): Promise<boolean> {\n    return await this.calculateButton.isVisible();\n  }\n\n  /**\n   * Checks if the 'Reset' button is visible.\n   * @returns A promise that resolves to true if the button is visible, false otherwise.\n   */\n  async isResetButtonVisible(): Promise<boolean> {\n    return await this.resetButton.isVisible();\n  }\n\n  /**\n   * Checks if the main calculator section is visible by verifying a key element.\n   * @returns A promise that resolves to true if the section is visible, false otherwise.\n   */\n  async isCalculatorSectionVisible(): Promise<boolean> {\n    return await this.monthSelect.isVisible(); // Using a prominent element as an indicator for the section\n  }\n}